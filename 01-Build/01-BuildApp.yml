jobs:

- job: 'BuildNodeJS'
  displayName: 'Build Job M'
  steps:
  # - task: NodeTool@1
  #   inputs:
  #     version: '16.x'
  #   displayName: 'Install Node.js'

  - script: |
      npm install
    displayName: 'npm install'

  - script: |
      npm run build
    displayName: 'npm build'
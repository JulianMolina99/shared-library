jobs:
- job: 'deploy'
  displayName: 'deploy app'
  steps:
  - powershell: |
      $env:BUILD_ID = "$(Build.BuildId)"
      docker-compose up -d
    displayName: 'Deploy with Docker Compose'

